spring:
  datasource:
    url: jdbc:postgresql://taller.postgres.database.azure.com:5432/jwt_security
    username: ta_admin
    password: TallerTac77
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect